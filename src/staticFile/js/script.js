function login() {
    const username = document.getElementById("username");
    const password = document.getElementById("password");
    username.value = "";
    password.value = "";
    username.focus();
    if (len(username) < 4 ) {
        pass
    }
    if (len(password) < 8) {
        pass
    }
}